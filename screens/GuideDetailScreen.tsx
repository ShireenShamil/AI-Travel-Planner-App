import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const GuideDetailScreen = () => {
  return (
	<View>
	  <Text>Guide Detail Screen</Text>
	</View>
  );
};

const styles = StyleSheet.create({
 
});

export default GuideDetailScreen;